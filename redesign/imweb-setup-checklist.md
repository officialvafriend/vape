# 액상덕후 — 아임웹 설정 체크리스트

> **설계 기준**: yohemite.com 메인 페이지 저장본(HTML 912KB, 인라인 CSS 486KB) 실측 분석
> **작업 순서**: 위에서부터 차례대로. 건너뛰면 뒤 단계에서 값이 틀어집니다.

## 이 문서 읽는 법

| 표기 | 뜻 |
|---|---|
| **값** | 요헤미티에서 실측한 정확한 수치입니다. 이대로 넣으세요. |
| *위치* | 아임웹 버전에 따라 메뉴 이름이 조금 다를 수 있습니다. 못 찾으면 알려주세요. |

⚠️ **시작 전 필수**: 아임웹 [설정 → 사이트 관리]에서 **사이트를 복제**해 테스트본을 먼저 만드세요.
헤더 형식을 바꾸면 기존 레이아웃이 초기화됩니다. 운영 중인 사이트에서 바로 하면 안 됩니다.

---

## STEP 1 — 공통 디자인 설정

*위치: 디자인 모드 → **공통 디자인 설정** (실제 화면 확인 완료)*

### 1-1. 색상

| 항목 | 값 | 비고 |
|---|---|---|
| **브랜드/링크색** | `#FF2D6F` | ★ 포인트 컬러. 이게 전역 대표색입니다 |
| 글자색 | `#111111` | 본문 |
| 배경색 | `#F3F2EF` | 오프화이트 지면 |
| 옵션 배지색 / 배지색 | 그대로 둠 | 상품 페이지 확정 후 조정 |

### 1-2. 레이아웃

| 항목 | 값 | 비고 |
|---|---|---|
| **본문폭** | **1080px** | ⚠️ 기본값 1280px에서 반드시 변경 |
| 그리드 간격 | 8px | 카드 간격이 16px로 보이는지 STEP 7에서 확인 후 조정 |

> **본문폭을 1280px로 유지하려면** 이미지 규격이 전부 달라집니다.
> 콘텐츠 실폭 1264px, 3열 카드 410px. STEP 8 표를 다시 계산해야 하니 미리 알려주세요.

### 1-3. 텍스트

| 항목 | 값 |
|---|---|
| 텍스트 정의 | **14px / 1.7** |
| 본문 한/중/일 | **Pretendard** |
| 본문 영문 | **Pretendard** |
| 제목 한/중/일 | **Pretendard** |
| 제목 영문 | 본문 글꼴에 따름 |

> ⚠️ **한돈 삼겹살체 같은 캐주얼 디스플레이 폰트는 쓰지 마세요.**
> 이 디자인은 각진 2px 검정 선 + 고채도 포인트의 날 선 룩입니다.
> 획이 두껍고 둥근 폰트를 쓰면 색을 아무리 맞춰도 긴장감이 무너집니다.

### 1-4. 구성요소

| 항목 | 값 |
|---|---|
| 버튼 스타일 | 배경 `#FF2D6F` · 글자 `#FFFFFF` · **모서리 각지게(radius 0)** |
| 입력 필드 | 모서리 각지게(radius 0) |

---

## STEP 2 — 포인트 컬러가 적용되는 범위

전역 포인트 컬러는 §1-1의 **브랜드/링크색** 하나입니다. 여기를 바꾸면
링크·버튼·활성 메뉴가 한꺼번에 따라옵니다. 다만 전부는 아닙니다.

| 어디 | 무엇 | 상태 |
|---|---|---|
| 공통 디자인 설정 → 브랜드/링크색 | 링크·버튼·활성 메뉴 | 관리자에서 해결 |
| 헤더 → 메뉴 위젯 설정 | GNB hover / active 세부 | 관리자에서 해결 |
| **커스텀 CSS (STEP 6)** | 드롭다운 2px 검정 테두리, 슬라이더 페이징, 푸터, 모바일 슬라이드 메뉴 | **CSS만 가능** |

> 브랜드/링크색을 설정하면 CSS의 §2·§4는 같은 값을 재확인하는 안전망이 됩니다.
> 충돌하지 않으니 그대로 두세요. 관리자 설정이 빠지거나 초기화돼도 CSS가 받쳐줍니다.

---

## STEP 3 — 헤더 (제일 중요)

*위치: 디자인 모드 → 헤더 영역 클릭 → 헤더 설정*

### 3-0. 시작 템플릿 — BLANK. (유틸바 + 로고 좌측형)

헤더 템플릿 목록에서 **국기/Login·Register·Cart·Order·Search 유틸바 + 아랫줄 좌측 `BLANK.` 로고**
형태를 고릅니다. 유틸바 + 1줄 헤더 + 로고 좌측, 세 가지가 요헤미티와 같아 손이 제일 덜 갑니다.

> 신형 헤더는 위젯을 좌/중앙/우 그룹으로 자유롭게 옮길 수 있습니다.
> 어느 템플릿을 골라도 결국 같은 모양을 만들 수 있고, 이건 시작점이 가까울 뿐입니다.

### 목표 배치 (요헤미티 실측)

```
┌──────────────────────────────────────────────────┐
│              [ 상단 알림 — 공지 문구 ]              │  30px · 배경 #141414
├──────────────────────────────────────────────────┤
│ [로고]    [ 구매하기  액덕랩  저널 … ]  [로그인│장바구니] │  63px · 배경 오프화이트
│ 좌 220px         중앙 (메뉴)          우 220px      │
└──────────────────────────────────────────────────┘
```

> **윗줄은 "상단 알림" 기능으로 갑니다. → 헤더는 1단입니다.**
> 요헤미티는 헤더 섹션에 이미지 위젯(1064px PNG, 카카오 채널 추가 링크)을 넣어
> 만들었지만, 문구가 텍스트 한 줄이면 상단 알림 기능이 훨씬 낫습니다.
> 문구를 바꿀 때마다 이미지를 다시 만들 필요가 없습니다.
> 그래픽이 들어간 배너가 필요해지면 그때 이미지 위젯 방식으로 되돌리면 됩니다.

**상단 알림 설정값**

| 항목 | 값 |
|---|---|
| 배경색 | `#141414` |
| 글자색 | `#FFFFFF` |
| 글자 크기 | 11~12px |
| 문구 | 예: `5만원 이상 무료배송 · 오후 3시 이전 결제 시 당일 출고` |
| 닫기 버튼 | 켜기 |

### 3-0-1. 템플릿 → 목표 배치 조정 (이 순서대로)

**① 유틸바 줄 삭제** — 국기·`Login Register Cart Order Search`·SNS 아이콘이 있던
윗줄 섹션을 통째로 삭제합니다. 로고가 있는 줄만 남겨 **1단 헤더**로 만듭니다.

**② 메뉴를 중앙으로** ★ 가장 중요
아랫줄 우측의 메뉴 위젯을 잡아 **중앙 컬럼으로 드래그**.

**③ 우측 유틸 구성** — 비어있는 우측 컬럼에 순서대로:
```
로그인 위젯 → 여백 → 구분선(hr) → 여백 → 장바구니 위젯
```
가운데 세로 구분선이 있어야 `로그인 │ 장바구니`로 떨어집니다.

**④ 로고** — 일단 텍스트로 `액상덕후`. 이미지 로고는 STEP 8에서 제작 후 교체.

### 3-1. 형식

| 항목 | 값 |
|---|---|
| 헤더 종류 | **신형 헤더** |
| 표시 방식 | **오버레이** (첫 섹션 위에 겹침) |
| 고정 | 켜기 (스크롤 시 상단 고정) |
| 구성 | **2단** — 유틸바 + 메인헤더 |
| 좌측 컬럼 폭 | **220px** 고정 |
| 우측 컬럼 폭 | **220px** 고정 |
| 중앙 컬럼 | 자동 (나머지 채움) |

### 3-2. 높이·배경

⚠️ **헤더는 처음부터 불투명합니다.** "투명했다가 스크롤하면 채워지는" 방식이 아닙니다.
오버레이란 헤더가 첫 섹션 *위에 얹힌다*는 뜻이고, 그래서 히어로 섹션 맨 위에
`여백 368px`을 넣어 헤더에 가리지 않게 밀어냅니다. (STEP 7 참고)

> **높이 입력란은 없습니다.** 아임웹 헤더 높이는 `상하 여백 × 2 + 위젯 높이`로 결정됩니다.
> 요헤미티 63px = `11 + 41(메뉴 높이) + 11`. 그래서 **상하 여백에 11**을 넣습니다.

**PC 메인헤더 (63px)** — 섹션 설정 화면의 필드 순서대로

| 필드 | 값 |
|---|---|
| 배경 | 채우기 |
| 배경색 | `#F3F2EF` |
| 텍스트 | `#111111` |
| 가로 100% 확장 | ☐ (1080px 유지) |
| 기본 좌우 여백 | ☐ **해제** → 좌우 여백 `8px` |
| 상하 여백 | **`11px`** |
| 위젯 좌우 간격 | `0px` |
| 세로 정렬 | 중앙 |
| 선두께 | **`0px`** |

> 선두께 주의: 템플릿 기본값 `1px #e7e7e7`이 들어있습니다.
> 요헤미티 헤더는 하단 선이 없습니다(`border-width: 0 0 0px` 확인).
> 남겨두면 헤더 아래 흐린 줄이 생겨 각진 느낌이 죽습니다.

### 3-3. 모바일 헤더 (53px)

아임웹 신형 헤더는 **PC와 모바일이 별개 섹션**입니다.
디자인 모드 상단의 PC/모바일 전환 아이콘으로 바꿔서 따로 편집합니다.

요헤미티 실측 구조 — 로고가 중앙이 아니라 **햄버거 옆 좌측**입니다.

```
┌─────────────────────────────────────┐
│ ☰  액상덕후                      🛒  │   53px · 배경 #F3F2EF
│ └─ 좌 80px ─┘            └ 우 80px ─┘│
└─────────────────────────────────────┘
```

| 위치 | 위젯 순서 |
|---|---|
| 좌 (80px) | 햄버거 → 여백 → 로고 |
| 우 (80px) | 장바구니 |

| 필드 | 값 |
|---|---|
| 배경색 | `#F3F2EF` |
| 텍스트 | `#111111` |
| 좌/우 컬럼 폭 | **80px** |
| 상하 여백 | **`2px`** (→ 높이 53px) |
| 좌우 여백 | `8px` (기본 좌우 여백 해제) |
| 위젯 좌우 간격 | `0px` |
| 세로 정렬 | 중앙 |
| 선두께 | **`0px`** |

> 모바일에도 유틸바 줄이 있으면 삭제합니다. 상단 알림이 모바일에서도 같이 뜹니다.
> 로고는 텍스트로 두세요. 53px 안에 들어가야 해서 PC 로고 이미지는 잘립니다.
> 모바일용 로고 이미지는 STEP 8에서 따로 뽑습니다.

### 3-3-1. 슬라이드 메뉴 (햄버거)

| 항목 | 값 |
|---|---|
| 배경 | `#F3F2EF` |
| 텍스트 | `#111111` |
| 1depth 글자 | **17px** |
| 2depth 글자 | **16px** |
| 현재 메뉴 | `#FF2D6F` |

> 관리자에서 대부분 조절되지 않습니다. 되는 것만 하고 넘어가세요.
> 커스텀 CSS `§8`이 전부 맞춥니다.

### 3-2-1. 헤더가 1단이 되면서 달라지는 값

| | 요헤미티 | 액상덕후 |
|---|---|---|
| 헤더 높이 | 93px (유틸바 30 + 메인 63) | **63px** |
| 히어로 섹션 상단 여백 | 368px | **338px** |

`368 - 30 = 338`. 헤더 아래로 보이는 빈 공간을 같게 맞추려면 30px을 빼야 합니다.
STEP 7 히어로 섹션에서 이 값을 씁니다. 상단 알림이 페이지를 밀어내는지 겹치는지에 따라
몇 px 차이가 날 수 있으니, 실제 화면 보고 미세조정하세요.

### 3-4. 컨테이너

| 항목 | 값 |
|---|---|
| 최대 폭 | **1080px** |
| 좌우 여백 | 8px |

### 3-5. 메뉴 타이포

| 항목 | 값 |
|---|---|
| 글자 크기 | **14px** |
| 굵기 | **Bold (700)** |
| 자간 | **0** |
| 좌우 여백 | 14px |
| 기본 색 | `#111111` |
| hover / 현재 페이지 | `#FF2D6F` |

> 💡 드롭다운의 **각진 2px 검정 테두리**는 관리자에서 안 나옵니다.
> `aekduk-custom.css` §3이 처리합니다 (STEP 6).

---

## STEP 4 — 푸터

*위치: 디자인 모드 → 푸터 영역 클릭*

| 항목 | 값 |
|---|---|
| 푸터 타입 | **타입 05** |
| 배경색 | `#141414` |
| 글자색 | `#FFFFFF` |
| 글자 크기 | **11px** |
| 정렬 | 가운데 |

**포함 항목**: 이용약관 · 개인정보처리방침(굵게) · 사업자정보 한 줄 · Copyright · "PC 모드로 보기" 버튼

---

## STEP 5 — 섹션 공통

*위치: 디자인 모드 → 각 섹션 선택 → 섹션 설정*

| 항목 | PC | 모바일 |
|---|---|---|
| 최대 폭 | **1080px** | 전폭 (extend) |
| 좌우 여백 | 8px | **0** |
| 기본 배경 | `#F3F2EF` | `#F3F2EF` |
| 구분 섹션 배경 | `#E7E5E0` | `#E7E5E0` |

⚠️ **모바일 섹션은 반드시 "전폭"으로.** 이미지가 화면 끝까지 닿아야 요헤미티 느낌이 납니다.

---

## STEP 6 — 커스텀 CSS 삽입

*위치: 디자인 → 코드 삽입 → HEAD 영역*

1. `redesign/aekduk-custom.css` 파일을 엽니다
2. 전체 복사
3. `<style>` 와 `</style>` 로 **감싸서** HEAD 영역에 붙여넣기
4. 저장 → 사이트 새로고침 → **드롭다운 메뉴에 마우스 올려서 확인**

검은 각진 테두리가 나오고 hover 시 핑크로 차면 성공입니다.

---

## STEP 7 — 섹션 조립

**⚠️ 핵심: PC와 모바일 섹션을 따로 만듭니다.**
요헤미티는 반응형 하나가 아니라 PC 전용 6개 + 모바일 전용 5개를 별도로 만들어 놨습니다.
각 섹션에서 "PC에서만 표시" / "모바일에서만 표시"를 지정하세요.

### PC 섹션 구성

| # | 섹션 | 위젯 순서 (위→아래) |
|---|---|---|
| 1 | **히어로** | 여백 368 → 이미지 `1064×213` (fadeIn) → 여백 299 |
| 2 | **제품** | 타이틀 `1064×52` → 여백 20 → 배너 `1064×553` → 카드 `344×464` ×3 (fadeInUp) → 여백 180 |
| 3 | **가이드** | 타이틀 `1064×125` → 여백 20 → 카드 `344×344` ×6 (3열 2행) → 여백 180 |
| 4 | **저널** | 타이틀 `1064×66` → 여백 15 → 갤러리 위젯 → 여백 120 |

히어로 섹션 배경: 풀블리드 배경이미지 (`background-size: cover`)

### 모바일 섹션 구성

| # | 섹션 | 위젯 순서 |
|---|---|---|
| 1 | **히어로** | 전폭 이미지 1장 |
| 2 | **제품** | 여백 80 → 타이틀 → 여백 30 → 이미지 스택 (간격 13~16) → 여백 100 |
| 3 | **가이드** | 여백 80 → 타이틀 → 여백 30 → 이미지 스택 (간격 12) → 여백 100 |
| 4 | **저널** | 갤러리 위젯 → 여백 80 |

### 위젯 애니메이션

*위치: 위젯 선택 → 애니메이션 탭*

| 대상 | 효과 | 시간 | 지연 |
|---|---|---|---|
| 히어로 이미지 | **fadeIn** | 0.6초 | 0 |
| 제품 카드 | **fadeInUp** | 0.6초 | 0 |
| 그 외 | 없음 | — | — |

> 요헤미티는 애니메이션을 아낍니다. 전부 다 움직이면 싸구려로 보입니다.

---

## STEP 8 — 이미지 규격

**모든 이미지는 2배수(@2x)로 제작 후 업로드하세요.** 레티나 화면에서 흐려집니다.

| 용도 | 제작 크기 | 업로드 |
|---|---|---|
| 히어로 배너 | 2128 × 426 | `1064×213` |
| 대형 배너 | 2128 × 1106 | `1064×553` |
| 제품 카드 | 688 × 928 | `344×464` |
| 가이드 카드 | 688 × 688 | `344×344` |
| 섹션 타이틀 | 2128 × 104 | `1064×52` |
| 저널 썸네일 | 2128 × 374 | `1064×187` |

### 이미지 안전영역

좌우 각 **40px**, 상하 각 **24px** 안쪽에 글자를 두세요. 모바일에서 잘립니다.

---

## STEP 9 — 최종 점검

- [ ] 오버레이 헤더가 히어로 위에 투명하게 겹치는가
- [ ] 스크롤하면 헤더 배경이 채워지는가
- [ ] 드롭다운이 **각지고 2px 검정 테두리**인가
- [ ] 드롭다운 항목 hover 시 **핑크 배경 + 흰 글씨**인가
- [ ] 모바일에서 이미지가 화면 끝까지 닿는가
- [ ] 아이폰 · 안드로이드 · 노트북 3종 확인
- [ ] 모바일 슬라이드 메뉴 1depth 17px / 2depth 16px

---

## 미확정 항목

**상품 리스트 · 상품 상세 레이아웃** — 요헤미티 상품 페이지 저장본이 아직 없습니다.
쇼핑몰의 진짜 핵심 화면이라 이것만 확보되면 나머지도 확정할 수 있습니다.

```
요헤미티 /product3 또는 상품 상세 페이지 접속
→ Ctrl+S (맥 Cmd+S)
→ "웹페이지, 전체" 형식으로 저장
```

CSS `§9. 쇼핑몰` 블록은 그때까지 잠정값입니다.
